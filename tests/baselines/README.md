# Visual reference review

Initial reference approval is pending. Browser journey artifacts contain review
screenshots. Never create or replace expected images without explicit owner review.
The visual gate fails when a required reference is absent; it never updates images
automatically. References use a fixed seed, camera, viewport and software backend.
