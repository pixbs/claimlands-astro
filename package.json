{
  "name": "claimlands-validation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {"test": "playwright test", "test:preview": "playwright test --grep deployed", "serve": "python -m http.server 4173 --bind 127.0.0.1 --directory dist"},
  "devDependencies": {"@playwright/test": "1.58.2", "wrangler": "4.129.1"}
}
